export default class CollisionSystem {
    update(entities) {
        // Her vil du implementere din kollisionsdetekteringslogik
        // For eksempel at tjekke for kollision mellem Snake og Food eller Snake med sig selv
        console.log('CollisionSystem update() called');
    }
}
